<%= render "shared/header" %>
<div id="wrapper">
  <div class="col-md-8">
  <%= month_calendar events: @nutrients do |date, nutrients| %>
    <%= date.day %>

    <% nutrients.each do |nutrient| %>
      <div>
        <%= "#{nutrient.name} #{nutrient.number}個" %>
      </div>
    <% end %>
  <% end %>
  </div>
  </div>
  </div>
</div>
<%= render "shared/footer" %>